继承？
父级有的子级也有

把父级的东西拿到子级

继承父级的属性、方法
1.属性：call、apply
2.方法：new

继承——

-------------------------------------------------------------------------------

面向对象——
1.用就行，不用管内部
2.不怎么用

防止命名冲突：
1.封闭空间
(function (){})();

2.命名空间

3.模块化——seajs

4.面向对象

Person.prototype.show
News.prototype.show

-------------------------------------------------------------------------------

MVC——后台

概念：分离(代码、分工)

1.前端
2.后台
3.编辑

M	Model		模型——数据
V	View		视图——表现(HTML/CSS/JS)
C	Controllor	控制器——领导

M——数据
V——表现
C——控制

-------------------------------------------------------------------------------

JS的MVC：
分工——

M——数据交互(ajax、jsonp)
V——页面表现(DOM操作、运动)
C——业务逻辑

绝大部分——框架
	AngularJS——强大、麻烦、不兼容(高级)
	backbone——够用、简单、兼容

-------------------------------------------------------------------------------

AngularJS：
数据
模板

-------------------------------------------------------------------------------

ng-app=""	范围——angular管谁	必有
ng-model	数据源
{{name}}	输出
ng-init		初始数据——;分隔

-------------------------------------------------------------------------------

M——数据
V——视图	html
C——控制器

-------------------------------------------------------------------------------













